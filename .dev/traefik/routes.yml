# routes.yml
http:
  routers:

    auth-router:
      rule: "Host(`iam.service.localhost`)"
      entryPoints:
        - "web"
        - "websecure"
      service: "auth-service"
      priority: 30

    fe-router:
      rule: "PathPrefix(`/`)"
      entryPoints:
        - "web"
        - "websecure"
      service: "fe-service"
      priority: 10

    rest-router:
      rule: "PathPrefix(`/api`)"
      entryPoints:
        - "web"
        - "websecure"
      service: "rest-service"
      priority: 20

  services:
    auth-service:
      loadBalancer:
        servers:
          - url: "http://172.17.0.1:8000"
    fe-service:
      loadBalancer:
        servers:
          - url: "http://172.17.0.1:6565"
    rest-service:
      loadBalancer:
        servers:
          - url: "http://172.17.0.1:6464"

tls:
  certificates:
    - certFile: /etc/traefik/cert.pem
      keyFile: /etc/traefik/key.pem
